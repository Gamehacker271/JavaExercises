if, if else, switch, for, while

Ej switch:

    String color = "Rojo";
    switch (color) {
        case "Azul":
            // Codigo del caso
            break;
        case "Rojo"
            // Codigo del caso
            break;
    }

